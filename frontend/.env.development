# Dipakai otomatis saat `npm run dev`.
# Vite dev server (localhost:5173) memanggil backend di localhost:5000 (beda port
# = cross-origin), sehingga URL absolut diperlukan dan CORS backend aktif di mode dev.
VITE_API_BASE_URL=http://localhost:5000
